import { staticValue } from "./static-value.function"

export const defaultAcl = staticValue('private')
export const defaultContentType = staticValue('application/octet-stream')
export const defaultMetadata = staticValue(null)
export const defaultCacheControl = staticValue(null)
export const defaultContentDisposition = staticValue(null)
export const defaultStorageClass = staticValue('STANDARD')
export const defaultSSE = staticValue(null)
export const defaultSSEKMS = staticValue(null)